server {
  listen 80;
  server_name _;
  root /usr/share/nginx/html;

  index index.html;

  location / {
    try_files $uri $uri/ /index.html;
  }

  # Optional: disable access logs to keep container output clean
  access_log off;
  error_log /var/log/nginx/error.log warn;
}
